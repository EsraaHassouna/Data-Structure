#include <stdio.h>
#include <stdlib.h>
#include "LinkedList.h"

int main()
{

    Add(13);
    Add(7);
    Add(10);
    Add(50;

    Display();

    printf("\n%d",GetCount());

    GetDataByIndex(2);

    printf("\n");

    InsertAfter(10, 7);

    Display();

    return 0;
}
